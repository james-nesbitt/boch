= COMMANDS HOOKS =

! Note that these are only run when commands are run from the
/command shell script.  They are not run if the command is
executed from a different source.  See the /command script
for more detail.
